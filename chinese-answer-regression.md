# 中文回答端到端复测

日期：2026-09-15。模型：deepseek-v4-flash。通过 Playwright 操作真实 Electron 窗口，逐条新建 Ask、填写问题、发送、点击新建 Note，并读取创建后的本地 Markdown。未伪造或直接写入对话历史，未修改模型设置或生产逻辑。

## 结果

9 条真实问题覆盖 8 种学习内容类型，均成功返回并创建 Note；对话及 Notes 已保留在 App 中。以下耗时为开始操作至回答出现，包含少量界面操作开销，不是纯模型推理时间。

| 内容 | 实际分类 | 耗时 | 回答 / Note |
| --- | --- | --- | --- |
| resilient 形容人的含义、用法及两个例句 | word | 15.2 秒 | 成功 / 成功 |
| take off 脱衣服、代词位置及两个例句 | phrase | 29.1 秒 | 成功 / 成功 |
| heavy rain 搭配及两个例句 | collocation | 21.2 秒 | 成功 / 成功 |
| ask sb to do sth 句型及两个例句 | pattern | 45.0 秒 | 成功 / 成功 |
| The book that you lent me was fascinating. 主干和定语从句 | sentence | 43.6 秒 | 成功 / 成功 |
| I had planned to walk home. However, it started to rain, so I took a taxi instead. 句间逻辑与衔接 | paragraph | 42.7 秒 | 成功 / 成功 |
| 现在完成时与一般过去时区分、三个例句 | comparison | 57.9 秒 | 成功 / 成功 |
| say / tell 区别、两个例句，明确要求英文回答 | comparison | 52.3 秒 | 成功 / 成功，仍按产品策略回答中文 |
| 现在完成时基本结构、主要用法及三个例句 | grammar_concept | 25.9 秒 | 成功 / 成功 |

原计划的语法问题包含明确的时态对比要求，因此识别为 comparison 合理；追加纯语法问题补足覆盖，并未将它作为 Router 错误。

## 自动化检查

9/9 通过：回答成功、中文语言元数据、预期输入/卡片类型、按 getSelectedCardModules 规范化后的模块顺序、中文二级标题、解释段落含中文、明确要求的例句数量及中文翻译、Note 完整等于实际渲染使用的 Markdown。词汇级语言检查不能证明整段语言质量，语义另行检查。

- 原始记录：[results.json](/tmp/englishask-playwright/results.json)
- 自动检查：[checks.json](/tmp/englishask-playwright/checks.json)
- 实际操作脚本：[regression.cjs](/tmp/englishask-playwright/regression.cjs)
- 校验脚本：[validate.cjs](/tmp/englishask-playwright/validate.cjs)

## 尚未达标

1. **语法规则限定不足。** 纯语法回答写道：“只要前面是 have/has，后面的形式就是过去分词。”应限定为现在完成时结构中的助动词 have/has；不能将这一表述泛化到所有 have/has 用法。对应 Ask：讲解现在完成时的基本结构和主要用法，给三个例句。
2. **段落分析展开不足。** 段落用例的“句子结构”只有第一句是独立分句的说明，没有展开第二句；结论虽给出正确的转折、因果、替代逻辑，模块内容仍不充分。语法部分还附加了未要求的另一段例句。对应 Ask：分析这段话的句间逻辑和衔接。
3. **跨模块重复仍存在。** take off 的代词必须放中间，在开头结论、语法和用法中重复；say/tell 的结构说明在结论、对比和用法中也有重复。say/tell 用法最后的“只有当…才…”语义接近条件也过于绝对，需另行限定。
4. **Note 表格内的行内 Markdown 未完全渲染。** say/tell 的 Note 表格将 `**to**` 原样显示，而不是加粗。表格网格及内容本身正常，保存的 Markdown 没有丢失。证据：[comparison-note.png](/tmp/englishask-playwright/comparison-note.png)。
5. **等待仍偏长。** 本轮为 15.2–57.9 秒，平均约 37.0 秒，中位数 42.7 秒。结束时主进程日志显示，say/tell 的语法审查耗时 36.1 秒，占该条 52.3 秒等待的较大部分；纯语法用例审查 8.4 秒且返回零修正，仍留下上述过度概括。9 条未发生超时，但没有完整的分阶段追踪，不能宣布偶发超时根因已解决。

## 范围限制

这是内容类型覆盖，不是所有 intent、所有提示表达或长期稳定性的穷举。未覆盖单独翻译、纠错、润色、发音、澄清模式，也未验证重试、并发和长会话。原生自动化尝试期间有一条重复输入的旧 resilient 对话，本轮未计入、未删除。

刷新后再次确认 9 条 Ask 和 9 份完全一致的 Note 均已持久保存。测试结束后关闭临时调试实例，恢复普通开发启动。

功能链路通过不等于语义全部通过；以上发现尚未在本轮修复。

## 2026-09-16：首批修复

已收紧 have/has 规则的助动词作用域，并要求段落分析覆盖所讨论逻辑关系的两端；审查局部语法错误时不得删掉其他相关分句。未增加模型调用阶段或改变审查触发范围。

718 项离线测试、构建和前端类型检查通过。最终 5 项真实模型回归通过，包括错误规则纠正、正确规则原样保留、段落修正保留上下文，以及语法和段落的真实生成。初轮发现正确句被无必要改写，已进一步收紧审查指导；第二轮的错误句引用误报通过改进测试断言解决，不将明确否定错误说法的回答判错。

- [修正后的语法回答](/tmp/englishask-coverage-verified-grammar-generation.json)：have/has 规则限定在完成时助动词结构中，保留三个中英配对例句。
- [修正后的段落回答](/tmp/englishask-coverage-verified-paragraph-generation.json)：覆盖步行计划、下雨、乘车及替代关系，不再附加无关新例句。
- [真实回归结果](/tmp/englishask-coverage-verified.log)：5/5 通过，63.38 秒。

这批定向真实测试未写入 App 历史；原来的 9 条 Ask 和 Notes 保留原样，以便对照。跨模块重复、Note 表格行内格式和等待时间尚未在本批修复；提示词改进仍需持续样本验证，不代表所有未来回答都不会出现语义错误。

## 2026-09-18：表格与模块分工验收

724 项离线测试通过（25 项可选测试跳过）。修正同义词断言后的真实模型测试 2/2 通过，耗时 89.02 秒；[测试日志](/tmp/englishask-ownership-0918.log)。take off 用法不再重复中置规则，语法限定到非重读人称代词；say/tell 用法补充场景和固定表达，不再重列整套结构公式。摘要与正文仍有信息重叠，本轮不宣称语义重复已完全消除。

Playwright 实际操作已有 say/tell Note，确认 `**to**` 正确显示加粗，点击进入源码后可见原始标记，失焦后恢复表格预览。前后读取 Markdown 完全相同，未修改用户笔记。[预览截图](/tmp/englishask-playwright/table-preview-0918.png)、[源码截图](/tmp/englishask-playwright/table-source-0918.png)。嵌套格式、HTML/危险链接与点击编辑由 DOM 回归覆盖。

临时调试端口已关闭，恢复普通开发启动。两条新模型样本只保存为临时测试结果，未写入 App 对话。等待时间优化和其他 intent 的真实回归仍待继续。

## 2026-09-18：等待时间分阶段基线

增加本地请求计时，记录阶段、模型、调用标识、耗时和成功/失败/取消状态，不记录问题、答案、密钥或错误正文。每次应用层调用独立计时，SDK 内部重试不单独拆分。

| 用例 | Router | 正文生成 | 语法审查 | 总耗时 |
| --- | ---: | ---: | ---: | ---: |
| resilient 形容人，两个例句 | 4.144 秒 | 13.973 秒 | 未触发 | 18.132 秒 |
| 现在完成时，三个例句 | 2.094 秒 | 14.175 秒 | 7.263 秒 | 23.536 秒 |

两个样本均通过类型、模块及例句数量检查，保留中文解释与配对翻译；未观察到应用层重复调用或阶段失败。正文生成是这两个样本最大的等待来源，不据此推断所有慢请求或服务商故障的原因。

728 项离线测试通过（27 项可选测试跳过），构建和前端类型检查通过。真实测试结果：[词汇](/tmp/englishask-latency-before-word.json)、[语法](/tmp/englishask-latency-before-grammar_concept.json)，未写入 App 历史。

本轮未降低推理强度、取消审查或改变模型参数。分阶段定位已完成，实际提速及更广的语义验证仍待后续工作；不能将这次采样与此前不同问题的耗时直接比较，宣称性能提升。

## 2026-09-18：补充意图回归与润色修复

新增翻译、纠错、润色、发音和缺少目标时澄清的真实链路测试。首轮五项机械检查通过，但人工读取样本发现：正确的邮件请求被附加了用户没有写过的错误句，并将其列为“真正的错误”。已收紧句子生成规则：实际错误必须来自原句；原句无误要明确说明；只有用户主动要求时才列假设错误。

修复后五项复测通过：

| 场景 | 核心检查 | 耗时 |
| --- | --- | ---: |
| 中译英：明天可能晚十分钟 | 保留可能性、时间和时长；无朗读目标 | 12.719 秒 |
| She go to school every day. | 最小纠错为 She goes to school every day. | 25.725 秒 |
| 礼貌邮件润色 | 保留 report 和 by Friday，承认原句正确 | 28.136 秒 |
| comfortable 美式发音 | intent、目标、音标及重音说明存在 | 6.661 秒 |
| 未提供待纠正句子 | 中文追问，不生成知识卡片 | 2.677 秒 |

润色复测的 Router 未选 common_mistakes，因此另外固定首轮模块组合做了定向补测，21.444 秒通过：该模块明确“原句没有语法错误”，未再列 Could you to send 或 send to me the report 这两条虚构错误。测试结果同时保留实际 Router 输出及固定后的分类，避免混淆真实路由与定向生成测试。[定向结果](/tmp/englishask-intents-fixed-0918-polishing-grounded.json)。

729 项离线测试、构建及前端类型检查通过；真实验证为五条完整链路加一条固定模块补测。测试仅保存临时 JSON，未写入 App 的 Asks/Notes。

范围仍有限：发音目标检查不等于音频听感或音标知识全部正确。首轮 comfortable 的音标模块使用 /ər/，说明另用 /ɝ/；复测另有“两点”后列四点的文字不一致。发音解释与音标一致性应另行优化，不能因为机械检查通过而算已修复。并发、重试、长会话以及实际提速仍待验证。
